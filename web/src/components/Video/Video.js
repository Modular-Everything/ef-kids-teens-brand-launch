import React from 'react';
import PropTypes from 'prop-types';

//

const Video = ({ videoData }) => <div />;

export default Video;

//

Video.propTypes = {
  videoData: PropTypes.string.isRequired,
};
